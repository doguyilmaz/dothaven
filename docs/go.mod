// Hugo module for the dothaven docs site. Isolated from the root Go module:
// `go build ./...` / `go test ./...` at the repo root skip this nested module.
module github.com/doguyilmaz/dothaven/docs

go 1.26.3

require github.com/imfing/hextra v0.12.3 // indirect
